pipeline {
  agent any
  triggers {
    GenericTrigger(
     genericVariables: [
      [key: 'ref', value: '$.ref']
     ]
    )
  }
  stages {
    stage('TEST') {
      steps {
        sh "echo $ref"
      }
    }
  }
}
